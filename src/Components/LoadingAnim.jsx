import React, { useState } from 'react';
import { useDispatch } from 'react-redux';
import './loadingAnim.css';

const LoadingAnim = () => {
    return(
        <div className='not-loading'>Loading</div>
    );
}

export default LoadingAnim;